apiVersion: v1
kind: Pod
metadata:
  name: bookstore-pod
spec:
  containers:
    - name: bookstore
      image: nginx:alpine
      envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secret
